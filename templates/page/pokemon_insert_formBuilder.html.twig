{% extends 'base.html.twig' %}


{% block title %}
    Pokemon : form builder
{% endblock %}

{% block main %}

    {{ form_start(pokemonForm) }}
        {{ form_row(pokemonForm.title) }}
        {{ form_row(pokemonForm.description) }}
        {{ form_row(pokemonForm.password) }}
    {{ form_end(pokemonForm) }}

            {# créer toute les imputs automatiquement a partire de la BDD #}
          {#   {{ form(pokemonForm) }}  #}



{% endblock %}